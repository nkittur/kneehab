/**
 * Single source of truth for the app's name. Renaming the app should mean
 * editing this file (plus the PWA manifest / index.html, which read from it).
 */
export const APP_NAME = 'Durable'
export const SHORT_NAME = 'Durable'
export const DESCRIPTION = 'Whole-body training and rehab planner'
